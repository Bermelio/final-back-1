<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Productos</title>
  <script src="https://cdn.tailwindcss.com"></script>
{{!-- Using PostCSS // USAR TAILDWIND COMO POSTCSS ARREGLARLO DESPUES --}}
</head>
<body class="bg-gray-100 text-gray-900">

  <div class="container mx-auto px-4 py-8">

    <h1 class="text-3xl font-bold mb-6 text-center"><a href="/products">Productos</a></h1>

    <div class="flex justify-end mb-4 gap-4">
      <a
        class="group relative inline-block overflow-hidden border border-indigo-600 px-4 py-2 focus:ring-3 focus:outline-hidden"
        href="/products?sort=asc" 
      >
      <span class="absolute inset-x-0 top-0 h-[2px] bg-indigo-600 transition-all group-hover:h-full"></span>
      <span class="relative text-sm font-medium text-indigo-600 transition-colors group-hover:text-white"> Precio ↓</span>
      </a>

      <a
        class="group relative inline-block overflow-hidden border border-indigo-600 px-4 py-2 focus:ring-3 focus:outline-hidden"
        href="/products?sort=desc"
      >
      <span class="absolute inset-x-0 top-0 h-[2px] bg-indigo-600 transition-all group-hover:h-full"></span>
      <span class="relative text-sm font-medium text-indigo-600 transition-colors group-hover:text-white">Precio ↑</span>
      </a>


      <a class="group relative inline-block overflow-hidden border border-indigo-600 px-4 py-2 focus:ring-3 focus:outline-hidden" href="/carts">
        <span class="absolute inset-y-0 left-0 w-[2px] bg-indigo-600 transition-all group-hover:w-full"></span>
        <span class="relative text-sm font-medium text-indigo-600 transition-colors group-hover:text-white">🛒 Ver carrito</span>
        <span id="cart-count" class="ml-1 inline-block bg-indigo-600 text-white rounded-full px-2 text-xs">0</span>
      </a>

    </div>


    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      {{#each products}}
        <a href="/products/{{this._id}}" class="block">
          <div class="bg-white rounded-2xl shadow-md p-4 flex flex-col justify-between">
            <div>
              <h2 class="text-xl font-semibold mb-2">{{this.title}}</h2>
              <div class="w-full h-64 relative overflow-hidden">
                <img src="{{this.thumbnail}}" alt="{{this.title}}" class="w-full h-48 object-scale-down rounded-lg mb-4">
              </div>
              <p class="text-gray-600 mb-1">Categoría: {{this.category}}</p>
              <p class="text-gray-800 font-bold mb-2">$ {{this.price}}</p>
              <p class="text-sm text-gray-500">Stock: {{this.stock}}</p>
            </div>
          </div>
        </a>
      {{/each}}
    </div>

    <!-- Paginación -->
    <div class="mt-8 flex justify-center items-center gap-4">
      {{#if hasPrevPage}}
        <a href="{{prevLink}}" class="px-4 py-2 bg-gray-300 rounded hover:bg-gray-400">&laquo; Anterior</a>
      {{/if}}

      <span class="font-semibold">Página {{page}}</span>

      {{#if hasNextPage}}
        <a href="{{nextLink}}" class="px-4 py-2 bg-gray-300 rounded hover:bg-gray-400">Siguiente &raquo;</a>
      {{/if}}
    </div>
  </div>

</body>
</html>
